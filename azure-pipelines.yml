# CI/CD for freyr-sales → AWS ECS Fargate.
# Prereqs (one-time, in Azure DevOps — see docs/aws-migration plan Task 12):
#   1. AWS Toolkit for Azure DevOps extension installed in the FreyrDevOps org.
#   2. An AWS service connection valid for the TARGET account (276654751635) — see awsCredentials below.
#   3. Variable group "freyr-sales-ci" with: AWS_REGION, ECR_REGISTRY, ECR_REPO,
#      ECS_CLUSTER, ECS_SERVICE, TASK_FAMILY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY.
trigger:
  branches:
    include:
      - freyrsales

pool:
  vmImage: ubuntu-latest

variables:
  - group: freyr-sales-ci

steps:
  - task: AWSShellScript@1
    displayName: Build, push, and deploy
    inputs:
      # NOTE: must be an ADO AWS service connection whose credentials target account 276654751635.
      # 'oneRIMS-Dev' was originally set up for account 338736353119 — confirm/replace it for the new account.
      awsCredentials: 'oneRIMS-Dev'
      regionName: '$(AWS_REGION)'
      scriptType: 'inline'
      inlineScript: |
        set -euo pipefail
        TAG=$(echo "$(Build.SourceVersion)" | cut -c1-7)
        IMAGE="$(ECR_REGISTRY)/$(ECR_REPO)"

        aws ecr get-login-password --region "$(AWS_REGION)" \
          | docker login --username AWS --password-stdin "$(ECR_REGISTRY)"

        docker build --platform linux/amd64 \
          --build-arg NEXT_PUBLIC_SUPABASE_URL="$(NEXT_PUBLIC_SUPABASE_URL)" \
          --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY="$(NEXT_PUBLIC_SUPABASE_ANON_KEY)" \
          -t "$IMAGE:$TAG" -t "$IMAGE:latest" .
        docker push "$IMAGE:$TAG"
        docker push "$IMAGE:latest"

        TD=$(aws ecs describe-task-definition --task-definition "$(TASK_FAMILY)" \
              --query 'taskDefinition' --output json)
        echo "$TD" | jq --arg IMG "$IMAGE:$TAG" '
          .containerDefinitions[0].image=$IMG
          | del(.taskDefinitionArn,.revision,.status,.requiresAttributes,
                .compatibilities,.registeredAt,.registeredBy)' > taskdef.json
        REV=$(aws ecs register-task-definition --cli-input-json file://taskdef.json \
              --query 'taskDefinition.revision' --output text)

        aws ecs update-service --cluster "$(ECS_CLUSTER)" --service "$(ECS_SERVICE)" \
          --task-definition "$(TASK_FAMILY):$REV" --force-new-deployment
        aws ecs wait services-stable --cluster "$(ECS_CLUSTER)" --services "$(ECS_SERVICE)"
