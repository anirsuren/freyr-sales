# Agent coverage and Mock-mode check — September 25, 2026

The Agent's permission-gated workspace reader currently covers Team, Meetings, Offerings, FDL Components, Market Intel, Leads, Opportunities, Solutioning, Contracts, Customers, Goals, and Reports. Answers about these records still depend on the user's module permissions and the source data present in the selected workspace. The earlier [50-question audit](agent-50-query-audit.md) covers representative questions, links, and permission boundaries in more depth.

The route inventory also includes Dashboard, Pipeline, Forecast, Analytics, Activity, Contacts, Sessions, Tasks, Campaigns, Sequences, Voice, Revenue Accruals, and several administration and planning pages. These are **not all independent Agent read modules**. Some information is reachable indirectly through the covered records, but the Agent cannot yet promise the same depth, calculations, or actions as every page. The Pipeline board is built from pitch sessions and estimates deal value from account size; the Forecast page also contains a synthetic Mock-mode rep roster. Those estimates must not be silently presented as recorded revenue or person-level facts. In particular, this pass did not verify page parity for those routes or enable CRM writes through the Agent. Headless CRM use therefore remains a product goal, not a completed capability.

Five paid Mock-mode questions checked goal names and monthly values, annual versus monthly targets, conversation handoff, and an Opportunities-plus-Solutioning answer. One test caught the model substituting Marketing Qualified Leads for the exact name “Marketing campaigns”; after the grounding change it correctly said the exact goal was absent in this Mock workspace. A Renewals follow-up returned August verified value of $3,799,268, zero pending/sent-back, and no monthly target, matching the underlying goal data. A cross-page question returned linked opportunities, owners, sign dates, original currencies, and linked or missing solutioning requests. The floating chat stayed open after page interaction, and opening full chat retained the tested conversation.

Limits: these checks do not establish that every page and every workspace datum is available to the Agent; a broad data-access claim would be false. The cross-page answer was inspected for rendering and links, but every returned record was not independently reconciled with its source page. The one-time retry reduces transient connection failures; it cannot guarantee external model availability. No load test, deployment, or production validation was performed. Further work should add permission-scoped readers and answer checks for the remaining high-use routes, starting with Pipeline, Forecast, Revenue Accruals, Sessions, Tasks, and Campaigns, then verify parity against their source pages with bounded questions.

## Minute-loop cursor: Mock Agent data parity

The next check caught a false absence claim. For the Freya.Label opportunity at Calyx Diagnostics, the Agent initially said there was no monthly revenue accrual schedule. The linked opportunity page actually shows nine months, March–November 2027, totaling $320,000 ($35,555 OTS and $284,445 ARR). The Agent's opportunity reader had omitted the separate accrual store. It now includes permission-gated plan existence and totals, and returns exact months for a named deal; denied or failed accrual reads are distinguished from no plan. A side-chat repeat gave all nine matching months and totals. Opening that same conversation in full chat preserved the answer. Typecheck, 18 focused workspace tests, lint and diff check passed locally.

The next full-chat question found two linked Solutioning requests. Both answer links opened the specific records in the same browser tab: the proposal is with Daniel Foster and in progress (the detail page labels it “Work in progress”); the completed meeting-support request is with Mark Miller. The exact opportunity links were present on both source pages. The side conversation remained open while navigating those links.

The second-deal check found a same-name link error. On the older `GRI. Lonza` deal (`/opportunities/seed-opp-50`), the page shows an existing accrual plan with zero months and zero scheduled dollars. The Agent described that state correctly but linked a different Mock deal also named `GRI. Lonza` (`/opportunities/fill10-opp-001-1`). The Agent now reads the exact permission-checked opportunity ID from the current page path and gives that record precedence for page-specific questions. Repeating the side-chat question returned the correct empty-plan state and linked `/opportunities/seed-opp-50`. The existing tab and conversation stayed in place. Typecheck, 19 focused workspace tests, lint and diff check passed locally.

The monthly-goal check found two failures. A June 2026 question for “Marketing Qualified Leads (MQLs) Generated” was first mistaken for a Leads-list question because the goal name contains “Leads.” After that routing fix, the Agent selected another goal with the same name, falsely reported zero June progress, and linked a record that did not open the selected organization goal. Named goal reads now expose the organization selection and recorded monthly group values; organization questions prefer the unique selected organization goal, and the prompt distinguishes cumulative milestones from monthly targets. The repeated side-chat answer reported June verified 105, pending 0, sent back 0; Focused Accounts AMR 46, Growth Accounts 35, and Focused Accounts EUA 24. It correctly said no June monthly target was set and identified the 300-lead cumulative milestone due June 30. Its progress visualization displayed the 105 recorded leads. Clicking the answer's link opened `/mock-mode/performance/goal/mock-mqls`, where the June organization row showed 105. Typecheck, 12 focused tests, focused lint, and diff check passed locally.

Next: continue bounded source-versus-Agent checks on the uncovered route inventory, beginning with Sessions or Tasks. Check both full and side chat over successive runs, including links and authorization. Use the existing Chrome tab only. Coverage remains partial; do not infer that all CRM data or actions are available through the Agent.

The June MQL answer exposed a chart usability issue even after its data was corrected: with no monthly target, the Agent drew one unlabeled green bar and offered no zoom or expanded view. Goal-progress answers now show a status-colored outcome rail with explicit verified, sent-back, and waiting values, plus the recorded total and an honest “no period target” label. The rail supports wheel/button zoom and drag-to-pan; every goal-progress chart has an expanded view. Targeted goals retain the Goals timeline, now interactive, while non-USD targets use the currency-aware rail rather than a USD label. The saved full-chat answer showed the corrected colors and labels, zoom changed to 1.6×, and the expanded chart opened and zoomed in the same existing tab. Typecheck, focused lint, and diff check passed. This is a visualization fix, not a claim that the June question has a monthly target.

The Sessions audit found an ungrounded answer. The Halcyon Biosciences row for Arjun Duarte on June 15 shows Interested, Regulatory Submission Services, Changes requested, and `/sessions/sess-fill-008`. The Agent first claimed Meeting Booked, Draft, and a fabricated URL. Sessions now has a permission-gated workspace reader using the same session, customer, contact and latest-contact-interaction stores as the page. Named-session questions get matching records before generation, and the exact primary service takes precedence over similarly related catalogue offerings. The final repeated side-chat answer used all three page labels and `/sessions/sess-fill-008`; clicking the link opened the actual Mock session detail page. Typecheck, 20 focused workspace tests, lint, and diff check passed locally. The list's Outcome field is the contact's latest interaction; it is not guaranteed to be an interaction within that session.

Next: investigate the Halcyon session detail's pitch script, which visibly addresses Dr. Mehta at BioNex while its account and contact are Halcyon Biosciences and Arjun Duarte. Determine whether this is a stale Mock seed shared by other sessions and fix the generic seed/display cause before treating the detail page as fully reliable. Then continue Tasks and other uncovered modules. The user requested the existing Chrome tab only, no production changes, and no questions while away.

The session-detail mismatch came from a shared BioNex sample pitch stored on many Mock sessions, not the detail renderer. Untouched sample scripts, intro emails, and call scripts now regenerate from each session's linked account, contact, and primary service. Existing edited pitches with version history are skipped, and the durable Mock store is migrated without reseeding other data. The shared generator also removes awkward company possessives and duplicate "Freyr from Freyr" sender text. The Halcyon page was retested in the existing tab: its three pitch formats address Arjun Duarte and Halcyon Biosciences, with Regulatory Submission Services. Seven focused Mock-density tests, typecheck, lint, and diff check passed. No email was sent and no live data changed.

The Tasks audit exposed another false absence claim. For Halcyon Biosciences and Arjun Duarte, the page contains one pitch review and three separate follow-up interactions. The Agent first repeated the page's generic “Needs review” badge and claimed there were no follow-ups. Tasks now has a permission-gated reader using the same saved sessions, contacts, customers and dated interactions as the queue. It exposes the exact saved “Changes requested” review state, counts the three dated follow-ups separately even though they open one contact, and supplies the actual session and contact URLs. A repeated Mock side-chat question returned all three follow-up dates and the saved review state. Both answer links opened the correct detail pages in the existing tab, and the side chat stayed open. Tasks do not store an owner, so personal ownership is not inferred from visibility.

That source-page check also found a calendar bug: the September 25 follow-up showed one day overdue on September 25, while its contact page said “in 1 day.” Bare `YYYY-MM-DD` values had been parsed as UTC instants. Tasks and the contact timeline now compare calendar dates. In the existing Mock tab, the September 25 Arjun row reads “Due today,” the contact page reads “today,” and the Tasks overdue count fell from 16 to 8. Twenty-two focused tests, typecheck, focused lint, and diff check pass locally. The Agent answer and both links were browser retested. Opening that side conversation in the full Agent chat preserved the corrected answer and its two links.

Next cursor: audit Campaigns or Sequences with a bounded source-versus-answer question. Continue the uncovered route inventory; do not infer that all CRM data or actions are available through the Agent.
