/**
 * WHAT THE ASSISTANT IS CALLED.
 *
 * Anir, Aug 27: "the name of the agent should be like Freyr AI or something.
 * It shouldn't just be agent — that's what the name of the person should be."
 *
 * "Agent" is what the thing IS, not who it is, and it was signing every
 * message with its job title. The product already calls it Freyr AI on the
 * offering pages ("Ask Freyr AI") and in the material access levels ("Freyr
 * AI Only"), so the chat was the one place still using the internal word.
 *
 * One constant, so a rename is one edit and never half-applied.
 */
export const AGENT_NAME = "Freyr AI";
